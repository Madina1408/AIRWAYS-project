export enum ToolTips {
    password = "Password must be min 6 characters (max 20), at least one digit, one uppercase letter, one lowercase letter, one special character (e.g. !, @, #, or ?)",
    name = "Add the passenger's name as it is written on their documents (passport or ID). Do not use any accents or special characters. Do not use a nickname."
}